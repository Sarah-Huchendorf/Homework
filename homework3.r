pizzaToppings = ['pepperoni','bacon','extra cheese','pineapples']
funtion greetCustomer(){
 
let greetCustomer = ${Welcome to Nickis Pizza, our toppings are}
    greetCustomer += $(toppings);
}
console.log(greetCustomer);    

function getPizzaOrder(size, crust, topping){
let order = ${order} ${size} ${crust}
crust pizza with; 
for(let topping of toppings){
    order += ${toppings
    console.log(order);
return [size, crust, toppings];
let customerOrder = get PizzaOrder(large, thin, pepporoni, bacon, extra cheese, pinapples)

function preparePizza(['orderSize, orderCrust orderToppings']){
    console.log("Your pizza is cooking.")
    return{
        size: orderSize,
        crust: orderCrust
        Toppings: orderToppings}
    
}
let cookedPizza preparePizza

let order2 = [1,2,3,]














































































































































    )
}
prints the order, i.e. "One large thick crust pizza with x, y, z, ... coming up!"
outputs a list with the size, crust, and toppings
Create a preparePizza function that
has an array as its parameter with three items: a size, a crust, and a list of toppings
prints something like "...Cooking pizza..."
outputs a pizza Object with appropriate key-value pairs for size, crust, and toppings
Create a servePizza function that
has a parameter of a pizza Object
logs a message that the pizza is ready and repeats the order, i.e. "Order up! Here's your large thick crust pizza with x, y, z, ... Enjoy!"
outputs the same pizza Object that was passed in
Call each function and (starting with preparePizza) use the returned value from the previous function as its input
Add, commit, and push your JS file to your GitHub repo.
Send your TA a link to your GitHub Homework repo when finished.